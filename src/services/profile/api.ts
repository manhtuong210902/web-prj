import { ROUTE } from "../axiosClient";
import { PREFIX, USER } from "../endpoints";

export const UPDATE_PROFILE = `${ROUTE}/${PREFIX}/${USER}/profile`;

export const UPDATE_AVATAR = `${ROUTE}/${PREFIX}/${USER}/profile/avatar`;

export const USER_PROFILE = `${ROUTE}/${PREFIX}/${USER}/profile`;
