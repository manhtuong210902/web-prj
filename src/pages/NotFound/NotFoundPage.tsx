import NotFound from "@src/components/Orther/NotFound";

const NotFoundPage = () => {
    return (
        <div className="w-full">
            <NotFound />
        </div>
    );
};

export default NotFoundPage;
